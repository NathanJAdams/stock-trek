[build-system]
requires = ["maturin>=1.0,<2"]
build-backend = "maturin"

[project]
name = "stock_trek"
version = "0.0.0" # placeholder
description = "Stock Trek time-series analysis python bindings"
authors = ["stock-trek.com <stock-trek@proton.me>"]
readme = "README.md"
requires-python = ">=3.8"
